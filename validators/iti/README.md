# @signature-kit/iti

ITI/PAdES conformance and remote validator package.

- `conformance` validates a PDF against ITI expected signing requirements.
- `remote` sends PDFs to the ITI Validar remote verifier and combines local conformance with remote verdicts.
