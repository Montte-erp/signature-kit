# SignatureKit workspace changelog

## Unreleased

- Added `konsistent` as a workspace structural linter and root `konsistent.json` conventions for source dependency-direction imports, barrel-only source modules, package directory shape, and remote-signer index surfaces.
- Kept content-level declarative checks in `tooling/static-checks` for rules that require source text or workspace manifest analysis.
