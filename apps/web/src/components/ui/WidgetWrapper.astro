---
const { id, class: className = "" } = Astro.props;
---

<section id={id} class={`border-t border-border py-16 md:py-24 ${className}`}>
  <div class="sw-container">
    <slot />
  </div>
</section>
