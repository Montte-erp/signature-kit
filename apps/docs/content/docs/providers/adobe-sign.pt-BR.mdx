---
title: Adobe Acrobat Sign
description: "Crie agreements no Adobe Acrobat Sign com createAdobeSignSignatureRequest(...), transientDocuments e erros tipados do SignatureKit."
---

O Adobe Acrobat Sign é um adapter de assinatura remota sobre a REST v6 API. O SignatureKit envia cada documento como um transient document e, em seguida, cria um agreement com esses transient IDs.

## Install

```package-install
@signature-kit/core @signature-kit/adobe-sign
```

## Create an agreement

```ts title="adobe-sign.ts"
import { createAdobeSignSignatureRequest } from "@signature-kit/adobe-sign"
import { signatureHttpClientLive } from "@signature-kit/core/http"
import { Effect, Redacted } from "effect"

const request = yield* createAdobeSignSignatureRequest(
  {
    accessToken: Redacted.make(process.env.ADOBE_SIGN_TOKEN ?? ""),
    baseUrl: "https://api.na1.echosign.com",
  },
  {
    title: "Service agreement",
    message: "Please review and sign the attached document.",
    documents: [{ fileName: "contract.pdf", mimeType: "application/pdf", content: pdfBytes }],
    recipients: [{ name: "Ana Souza", email: "ana@example.com", role: "signer" }],
    send: true,
    redirectUrl: "https://app.example.com/signed",
  },
).pipe(Effect.provide(signatureHttpClientLive))

// request.provider === "adobe-sign"
// request.state    === "sent" | "draft"
```

## Request mapping

- Cada documento é enviado para `/api/rest/v6/transientDocuments` como `File` multipart.
- O agreement é criado em `/api/rest/v6/agreements` com `fileInfos[]`.
- `role: "approver"` é mapeado para `APPROVER`; todos os demais recipients são mapeados para `SIGNER`.
- `send: false` é mapeado para o `DRAFT` do Adobe; o padrão é mapeado para `IN_PROCESS`.
- `redirectUrl` é mapeado para `postSignOption.redirectUrl`.

<Callout type="warn" title="Assunto e expiração">
  O adapter atual envia `message` e as opções de redirecionamento para o Adobe. Ele ainda não mapeia `subject` nem `expiresAt`, porque esses campos não compartilham o mesmo formato entre os provedores.
</Callout>

## Alchemy

```ts title="alchemy.ts"
import * as AdobeSign from "@signature-kit/adobe-sign"
import { signatureHttpClientLive } from "@signature-kit/core/http"
import { Layer, Redacted } from "effect"

export const providers = AdobeSign.providers({
  accessToken: Redacted.make(process.env.ADOBE_SIGN_TOKEN ?? ""),
}).pipe(Layer.provide(signatureHttpClientLive))
```

## Errors

Entradas inválidas, falhas HTTP remotas e respostas com formato inesperado falham como `SignatureKitError`, com `provider: "adobe-sign"` e metadados estruturados.

<Cards>
  <Card title="Dropbox Sign" href="/docs/providers/dropbox-sign">O adapter do Dropbox Sign sobre signature_request/send.</Card>
  <Card title="Remote signers" href="/docs/providers">Volte para todos os pacotes de provedores.</Card>
</Cards>
