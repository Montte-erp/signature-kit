---
title: Documenso
description: "Crie envelopes v2 do Documenso com createDocumensoSignatureRequest e uploads multipart de PDF."
---

O Documenso é uma plataforma de assinatura open-source. `createDocumensoSignatureRequest` monta um envelope v2 a partir dos seus arquivos PDF e o distribui quando `send !== false`.

## Instalação

```package-install
@signature-kit/core @signature-kit/documenso
```

## Crie e distribua um envelope

Os exemplos públicos do Documenso passam o token de API diretamente em `Authorization`, que é o padrão do SignatureKit. Se a sua instância espera `Bearer <token>`, defina `authorizationScheme: "bearer"`.

```ts title="documenso.ts"
import { createDocumensoSignatureRequest } from "@signature-kit/documenso"
import { signatureHttpClientLive } from "@signature-kit/core/http"
import { Effect, Redacted } from "effect"

const request = yield* createDocumensoSignatureRequest(
  {
    apiKey: Redacted.make(process.env.DOCUMENSO_API_KEY ?? ""),
    baseUrl: "https://app.documenso.com/api/v2",
  },
  {
    title: "Service agreement",
    subject: "Please sign",
    message: "Review and sign this agreement.",
    documents: [{ fileName: "agreement.pdf", mimeType: "application/pdf", content: pdfBytes }],
    recipients: [{ name: "Ana Silva", email: "ana@example.com", role: "approver", routingOrder: 1 }],
    redirectUrl: "https://app.example.com/done",
  },
).pipe(Effect.provide(signatureHttpClientLive))

// request.provider === "documenso"
// request.state    === "sent" | "draft"
```

## Mapeamento da requisição

- `POST /envelope/create` recebe o `payload` multipart mais uma parte `files` por documento.
- Os `recipients` são mapeados para papéis do Documenso: `approver` vira `APPROVER`; o padrão vira `SIGNER`.
- `routingOrder` é mapeado para `signingOrder`.
- `subject`, `message` e `redirectUrl` são mapeados para o `meta` do Documenso.
- Quando `send` é omitido ou true, o SignatureKit chama `POST /envelope/distribute` e retorna a primeira `signingUrl`.
- Quando `send: false`, o SignatureKit para após a criação e retorna uma requisição em rascunho.

<Callout type="info" title="Formato de expiração">
  O Documenso modela a expiração como um período de validade do envelope. O adapter não mapeia `expiresAt` — defina a expiração no Documenso ou estenda as opções do provider para controle exato.
</Callout>

## Alchemy

```ts title="alchemy.ts"
import * as Documenso from "@signature-kit/documenso"
import { Redacted } from "effect"

export const providers = Documenso.providers({
  apiKey: Redacted.make(process.env.DOCUMENSO_API_KEY ?? ""),
})
```

## Erros

Entradas inválidas, falhas HTTP remotas e falhas no formato da resposta resultam em `SignatureKitError` com `provider: "documenso"` e metadados estruturados.

<Cards>
  <Card title="DocuSeal" href="/docs/providers/docuseal">O adapter de assinatura remota open-source DocuSeal.</Card>
  <Card title="Assinaturas remotas" href="/docs/providers">Volte para todos os pacotes de provider.</Card>
</Cards>
