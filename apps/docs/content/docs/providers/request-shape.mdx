---
title: Provider request shapes
description: "Each provider package exposes the request facts its upstream API supports."
---

Provider APIs create upstream signing ceremonies. They are not local `Signatures` backends: the provider owns links, notifications, recipients, envelopes, and audit UX. SignatureKit keeps the calls boring: typed input, Redacted credentials, `SignatureHttpClient`, and `SignatureKitError` on recoverable failures.

```package-install
@signature-kit/http @signature-kit/clicksign @signature-kit/assinafy @signature-kit/zapsign @signature-kit/docuseal @signature-kit/documenso
```

## Providers

<Cards>
  <Card title="Clicksign" href="./clicksign">v1 document + signer flow, one document per request.</Card>
  <Card title="Assinafy" href="./assinafy">Document, signer, and assignment inside an account.</Card>
  <Card title="ZapSign" href="./zapsign">Bearer-token PDF upload with first signing URL.</Card>
  <Card title="DocuSeal" href="./docuseal">Open-source PDF submissions with submitter links.</Card>
  <Card title="Documenso" href="./documenso">Open-source v2 envelopes with optional distribution.</Card>
</Cards>

## Provider-owned request shapes

Each signer package defines the documents, recipients, routing, status values, and limits its upstream API actually supports. Clicksign, Assinafy, and ZapSign encode their single-document constraints in their local resource-props schemas; DocuSeal and Documenso keep multi-document provider models.

<Callout type="info" title="Local vs remote">
  Use `@signature-kit/pdf` + `Signatures` when SignatureKit must sign bytes. Use provider request functions when an upstream platform owns the ceremony.
</Callout>
